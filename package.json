{
  "name": "kumaran-portfolio-monorepo",
  "version": "1.0.0",
  "description": "Production Monorepo wrapper for Kumaran R P's Portfolio",
  "scripts": {
    "setup": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client",
    "start": "node server/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
